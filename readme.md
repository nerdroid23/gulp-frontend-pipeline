# Skeleton Gulp init

## Installation
```bash
# clone the repo
$ git clone <paste repo link here>
```

## Build Setup

```bash
# install dependencies
$ npm install

# compile and watch files
$ npm run watch

# build for production
$ npm build
```
